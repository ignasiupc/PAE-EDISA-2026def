# Python dependencies for the QR/barcode detection scripts in this folder.
#
# Important: pyzbar also requires the native zbar library.
# pip cannot install that system dependency from requirements.txt.
# Install it manually before running the scripts:
#
# macOS:
#   brew install zbar
#
# Debian / Raspberry Pi OS:
#   sudo apt install -y libzbar0

numpy==2.4.4
opencv-python==4.13.0.92
pyzbar==0.1.9
